<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardElevation="10dp"
    app:cardBackgroundColor="@color/white">
    <RadioGroup
        android:id="@+id/rgStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp">

        <RadioButton
            android:id="@+id/rbDraft"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="20sp"
            android:textStyle="normal"
            android:textColor="@color/gradientEnd"
            android:buttonTint="@color/gradientEnd"
            android:paddingHorizontal="20dp"
            android:text="Draft"/>

        <RadioButton
            android:id="@+id/rbOnConfirmation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="20sp"
            android:textStyle="normal"
            android:textColor="@color/gradientEnd"
            android:buttonTint="@color/gradientEnd"
            android:paddingHorizontal="20dp"
            android:text="On confirmation"/>

        <RadioButton
            android:id="@+id/rbOnExternalConfirmation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="20sp"
            android:textStyle="normal"
            android:textColor="@color/gradientEnd"
            android:buttonTint="@color/gradientEnd"
            android:paddingHorizontal="20dp"
            android:text="On external confirmation"/>

        <RadioButton
            android:id="@+id/rbSigned"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="20sp"
            android:textStyle="normal"
            android:textColor="@color/gradientEnd"
            android:buttonTint="@color/gradientEnd"
            android:paddingHorizontal="20dp"
            android:text="Signed"/>


    </RadioGroup>

</androidx.cardview.widget.CardView>